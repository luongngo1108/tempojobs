import { ProvinceConvertPipe } from './province-convert.pipe';

describe('ProvinceConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new ProvinceConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
